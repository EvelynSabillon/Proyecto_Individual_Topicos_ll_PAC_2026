# Datos de conexion con el router MikroTik.
# Lo escribe la pagina 'Conexion y llaves SSH' de la aplicacion, pero se
# puede editar a mano con cualquier editor de texto.
ip = 192.168.56.10
usuario = admin
llave = ~/.ssh/mikrotik_tea_key
interfaz1 = ether1
interfaz2 = ether2
